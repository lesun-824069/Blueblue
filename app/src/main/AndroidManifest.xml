<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gao.blueblue">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".AppContext"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Blueblue">
        <activity android:name=".ui.activity.addressbook.NewFriendApplyActivity"></activity>
        <activity android:name=".ui.activity.addressbook.PersonalInfoActivity" />
        <activity android:name=".ui.activity.login.LoginBySMSActivity" />
        <activity android:name=".ui.activity.login.LoginActivity" />
        <activity android:name=".ui.activity.settings.SettingsActivity" />
        <activity android:name=".ui.activity.base.BaseActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".ui.activity.start.StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>